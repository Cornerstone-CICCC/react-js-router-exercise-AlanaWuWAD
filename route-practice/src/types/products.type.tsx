export interface Product {
    id: string,
    title: string,
    category: string
}

export interface ProductsDetail {
    id: string,
    title: string,
    category: string
    description: string,
    price: string,
    rating: string
}